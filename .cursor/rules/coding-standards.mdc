---
alwaysApply: true
---

# Стандарты кодирования и руководство по стилю 📝

## Соглашения об именовании

### Go код
- **Пакеты**: строчные буквы, предпочтительно одно слово (например, `telegram`, `artificial`, `persistence`)
- **Экспортируемые типы**: PascalCase (например, `TelegramBot`, `AIProvider`)
- **Неэкспортируемые типы**: camelCase (например, `messageHandler`, `configReader`)
- **Функции**: PascalCase для экспортируемых, camelCase для приватных
- **Переменные**: camelCase, короткие имена в коротких областях видимости
- **Константы**: UPPER_SNAKE_CASE (например, `MAX_RETRY_COUNT`, `DEFAULT_TIMEOUT`)

### Файлы и директории
- **Предпочтительно**: Одно слово когда возможно (например, `handler.go`, `config.go`)
- **Приемлемо**: Два слова в snake_case формате (например, `message_handler.go`)
- **Избегать**: Три и более слов - попробуйте найти более короткие альтернативы

## Правила именования пакетов
- **Строгое правило**: Всегда только одно слово
- **Примеры**: `telegram`, `artificial`, `persistence`, `repository`, `platform`

## Язык и комментарии
- **Комментарии к коду**: Используйте русский язык только когда действительно оправдано (например, объяснение обходных решений или хаков)
- **Commit сообщения**: Русский язык, формальный стиль, краткое описание изменений, без излишних деталей
- **Документация**: Русский язык для внутренней документации, английский для публичных API

## Принципы стиля кода

### Go специфичные принципы
- Используйте современные возможности Go (1.23+)
- Следуйте идиомам Go
- Используйте структурированное логирование с slog
- Применяйте Uber-Fx для dependency injection
- Предпочитайте неизменяемые структуры данных
- Держите функции маленькими и сфокусированными
- Используйте осмысленные имена переменных и функций

### Архитектурные принципы
- **Модульная архитектура**: Каждый модуль отвечает за определенную функциональность
- **Dependency Injection**: Используйте Uber-Fx для управления зависимостями
- **Repository Pattern**: Абстрагируйте доступ к данным через репозитории
- **Структурированное логирование**: Всегда используйте slog для логирования
- **Обработка ошибок**: Явная обработка всех ошибок, используйте error wrapping

### Специфика проекта Xi Manager
- **AI интеграции**: Единообразный интерфейс для всех AI провайдеров
- **Telegram Bot**: Четкое разделение команд и обработчиков сообщений
- **База данных**: Используйте GORM с автогенерацией DAO через GORM-gen
- **Кэширование**: Redis для сессий и временных данных
- **Конфигурация**: Переменные окружения через platform/environ.go

## Форматирование кода
- **Go**: Используйте `gofmt` и `goimports`
- **Отступы**: Табы для Go (стандарт языка)
- **Длина строк**: Разумная длина, избегайте чрезмерно длинных строк
- **Группировка импортов**: Стандартная библиотека, внешние пакеты, локальные пакеты

## Документирование
- **GoDoc**: Документируйте все экспортируемые функции, типы и константы
- **Русский язык**: Для внутренней документации и комментариев
- **Примеры**: Включайте примеры использования для сложных функций
- **Контекст**: Объясняйте "почему", а не только "что"