---
globs: *.c, *.h
---

# C Files (.c, .h) Rules 🔧

## Основные принципы
- Следуйте стандарту C99 или новее
- Используйте четкие и описательные имена
- Применяйте принципы модульности
- Обеспечьте переносимость кода

## Стиль кодирования

### Именование
- **Функции**: snake_case (например, `process_message`, `init_config`)
- **Переменные**: snake_case (например, `user_count`, `buffer_size`)
- **Константы**: UPPER_SNAKE_CASE (например, `MAX_BUFFER_SIZE`, `DEFAULT_PORT`)
- **Макросы**: UPPER_SNAKE_CASE (например, `ARRAY_SIZE`, `MIN`, `MAX`)
- **Структуры**: snake_case с суффиксом `_t` (например, `message_t`, `config_t`)

### Форматирование
- **Отступы**: 4 пробела (не табы)
- **Скобки**: K&R стиль или Allman стиль (единообразно в проекте)
- **Длина строк**: Максимум 180 символов

### Пример стиля K&R
```c
int process_message(const char *input, char *output, size_t output_size) {
    if (input == NULL || output == NULL) {
        return -1;
    }
    
    // Обработка сообщения
    for (size_t i = 0; i < strlen(input); i++) {
        // логика обработки
    }
    
    return 0;
}
```

## Структура заголовочных файлов (.h)

### Header Guards
Используйте include guards для предотвращения множественного включения:

## Структура исходных файлов (.c)

### Порядок секций
1. Включения системных заголовков
2. Включения пользовательских заголовков
3. Локальные константы и макросы
4. Локальные типы данных
5. Статические функции (объявления)
6. Глобальные функции
7. Статические функции (определения)

## Управление памятью

### Принципы
- Всегда освобождайте выделенную память
- Проверяйте возвращаемые значения malloc/calloc
- Устанавливайте указатели в NULL после освобождения

## Обработка ошибок

### Возвращаемые значения
- Используйте отрицательные значения для ошибок
- 0 для успешного выполнения
- Положительные значения для специальных состояний

## Специфика проекта Xi Manager

### Интеграция с Go
- Если C код используется через CGO, следуйте соглашениям CGO
- Используйте `extern "C"` для C++ совместимости
- Обеспечьте корректную обработку строк UTF-8

## Best Practices
- Используйте const для параметров, которые не изменяются
- Предпочитайте статические функции для внутренней логики
- Избегайте глобальных переменных
- Используйте осмысленные имена переменных
- Проверяйте границы массивов
- Инициализируйте переменные при объявлении
- Используйте typedef для сложных типов