---
globs: Dockerfile*
---

# Docker Files (Dockerfile*) Rules 🐳

## Основные принципы
- Используйте многоэтапную сборку (multi-stage builds) для оптимизации размера образа
- Минимизируйте количество слоев
- Используйте официальные базовые образы
- Применяйте принцип наименьших привилегий

## Структура Dockerfile

### Базовый образ
- Используйте официальные Go образы для сборки: `golang:1.23-alpine`
- Для runtime используйте минимальные образы: `alpine:latest` или `scratch`

## Оптимизация производительности

### Кэширование слоев
- Копируйте `go.mod` и `go.sum` перед остальными файлами
- Выполняйте `go mod download` перед копированием исходного кода
- Используйте `.dockerignore` для исключения ненужных файлов

### Размер образа
- Удаляйте пакетные кэши: `rm -rf /var/cache/apk/*`
- Объединяйте RUN команды для уменьшения слоев
- Используйте `--no-cache` флаг для apk

## Best Practices
- Используйте COPY вместо ADD, если не нужны дополнительные возможности ADD
- Группируйте связанные RUN команды
- Очищайте кэши пакетных менеджеров в той же RUN команде
- Используйте HEALTHCHECK для мониторинга состояния контейнера
- Документируйте порты через EXPOSE, даже если они не обязательны