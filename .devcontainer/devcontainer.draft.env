# =============================================================================
# SECRETS - Copy this file to .devcontainer.draft and fill in your values
# =============================================================================
# This file is for local development only.

# -----------------------------------------------------------------------------
# Telegram Bot
# -----------------------------------------------------------------------------
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# -----------------------------------------------------------------------------
# AI API Keys
# -----------------------------------------------------------------------------
OPENROUTER_API_KEY=your_openrouter_api_key_here
OPENAI_API_KEY=your_openai_api_key_here

# -----------------------------------------------------------------------------
# Proxy (HTTP/SOCKS5)
# -----------------------------------------------------------------------------
PROXY_ADDRESS=socks5://proxy.example.com:1080
PROXY_USER=proxy_username
PROXY_PASS=proxy_password

# -----------------------------------------------------------------------------
# Agent Prompts (base64 encoded)
# -----------------------------------------------------------------------------
AGENT_CONTEXT_SELECTION_PROMPT=base64_encoded_prompt
AGENT_MODEL_SELECTION_PROMPT=base64_encoded_prompt
AGENT_RESPONSE_LENGTH_PROMPT=base64_encoded_prompt
SUMMARIZATION_PROMPT=base64_encoded_prompt
AGENT_PERSONALIZATION_VALIDATION_PROMPT=base64_encoded_prompt
AGENT_PERSONALIZATION_EXTRACTION_PROMPT=base64_encoded_prompt
AGENT_WEB_SEARCH_PROMPT=base64_encoded_prompt